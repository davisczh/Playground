import * as THREE from 'three';

export const wallMaterial = new THREE.MeshBasicMaterial({ color: 0x00000, side: THREE.FrontSide });
export const floorMaterial = new THREE.MeshBasicMaterial({ color: 0xededed, side: THREE.FrontSide });
